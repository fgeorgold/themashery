= link_to("My Account", account_path) if current_user
= "My Account" unless current_user
= "| " + link_to("Log Out", logout_path) if current_user
= "| " + link_to("Log In", login_path) unless current_user
= "| " + link_to("Register", register_path) unless current_user