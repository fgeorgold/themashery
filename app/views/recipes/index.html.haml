.content-header
  %h1 Recipes

.content-body
  %table
    %tr
    - for recipe in @recipes
      %tr
        %td
          = recipe.title
        %td
          = link_to 'Show', resource(recipe)
        %td
          = link_to 'Edit', resource(recipe, :edit)

.content-footer
  = link_to 'New', url(:new_recipe)

